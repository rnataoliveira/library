@model Reservation

@if(TempData["ReservationSuccess"] != null) 
{
    <h1 class="list-group-item mt-5" style="background-color: lightgreen">
        <span>@TempData["ReservationSuccess"]</span>
    </h1>
}
<div class="row justify-content-center">
    <div class="col-8 card mt-5">
        <div class="card-block">
            <h4>Número da Reserva: @Model.Number</h4>
        </div>    
    </div>
</div>
<div class="row justify-content-center mt-2">
    <div class="col-2 ">
        <a class="btn btn-primary">Ver minhas reservas</a>
    </div>
</div>
@section SearchForm {
    @Html.Partial("~/Features/BookCatalog/SearchForm.cshtml")
}